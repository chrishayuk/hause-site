# Experiments before explanations

9 September 2026. Local implementation, not a deployment.

## The three journeys

- Choosing uses the donated DecisionTrail's `beside` layout. Intent and act choices are compact native disclosures adjacent to the selected study on wider screens; narrower screens stack them above it. All options remain links and the full grammar remains in HTML.
- GraphNeighbourhood groups edges by relation and neighbouring record kind. Branches larger than six edges start collapsed. Nothing is truncated: all edges, directions, source links and bases are retained. The new grouping helper has a losslessness test.
- In practice replaces its repeated need/summary/contribution paragraphs with a short takeaway. The dated contribution and its linked records remain in a disclosure. Original media hashes are unchanged.

## Problem chapters

The experiment now precedes the longer symptom/cause prose. Answer, demonstration, cause and citation anchors remain. The original tutorial/reference Lens and its three depth records are preserved.

| Chapter | Encounter | Scope |
| --- | --- | --- |
| Containers | Same record, two presentations | Fictional gallery; identical assertion, support and boundary in both treatments |
| Refusal | Request a present plan or absent inspection report | Illustrative archive; no substituted report |
| Machine reading | Designed / plain text / structured record | One fictional listening dataset; qaLd output is displayed, not injected into the actual page metadata |
| Citation | Restore attribution; select and download a reference | Actual chapter record, including immutable first-publication date and a separate revision |
| Drift | Compare identities, damage a copy, restore | Pinned manifest versus ingested documentation; not independent measurements and not a live filesystem mutation |
| Tutorial/reference | Learn / Inspect / Spec | Existing approved Lens |
| Certainty | Reveal responses, refute “every”, narrow the claim | Ten fictional responses, eight A and two B; no population inference |
| Motion | Establish / exit / hold / enter / rest | Original CSS paper study, not a physics simulation; explicit reader-started finite sequence |

The problem page scope now makes Reveal visible without JavaScript and sets OPEN, SUPPORTED and REFUTED labels to the current foreground. Labels—not colour alone—carry status. Paper animation only runs while SequencePlayer reports playback, honours reduced motion, and stops offscreen or when the page is hidden. The complete score remains in a native disclosure. No competing video is embedded in this standalone sequence.

## Source and library record

SequencePlayer and recordDifference are donated to the library; DecisionTrail and GraphNeighbourhood are refined there. The exact ten-file checked source snapshot remains the compatibility bridge. No package upgrade or taxonomy change: the site still has 35 semantic forms. Inventory differences detect missing, unexpected and equal-count substituted identities, rejecting duplicates. Domain records and CSS art direction remain site-owned.

Problem publication records are revision 2026-09-09, version 1.1, retaining original publication dates. Publication capabilities are version 1.4. Knowledge records now describe the actual demonstrations and attribute the two new helpers to their originating problem chapters, not CHRISHAYUK. Graph totals: 81 nodes, 253 edges.

## Checks

- Production build, source suites and built-page suites pass.
- `scripts/problem-coverage.ts` checks every chapter's one h1, unique IDs, stable fragment targets, experiment order, native radio options, exact real citation downloads, explicit damaged-copy results and five-frame SSR resting state.
- `scripts/journey-coverage.ts` covers all 35 selections and every connected record, including grouped edge preservation.
- Twelve portable library tests and README file coverage pass.
- Runtime HTML checks are available through each script's `--url=http://127.0.0.1:3010` option; these are HTTP checks, not simulated browser interactions.

Browser discovery again returned no available browser. Visual layout, keyboard operation and actual playback still require browser sign-off before publication. Source/CSS and served HTML checks are not presented as screenshot or interaction verification.
